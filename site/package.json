{
  "name": "wesc-site",
  "version": "0.0.0",
  "private": true,
  "description": "Marketing/docs site for WeSC, served from a Rust Cloudflare Worker.",
  "scripts": {
    "dev": "node scripts/dev-site.mjs",
    "dev:worker": "wrangler dev --live-reload",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "shiki": "^4.2.0",
    "wrangler": "^4"
  }
}
